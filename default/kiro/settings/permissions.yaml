rules:
  - capability: skill
    effect: allow
  - capability: web_search
    effect: allow
  - capability: web_fetch
    effect: allow
  - capability: subagent
    effect: allow
  - capability: mcp
    effect: allow
