history.jsonl
# agents/ is mostly AIM-managed drops; track only the ones we author.
# Needs the `agents/*` form — an excluded *directory* can't be re-included.
agents/*
!agents/parallel-writer.md
backups
briefs
file-history
plugins
projects
session-env
sessions
shell-snapshots
tasks
